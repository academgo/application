import { TabsBlock } from "@/types/blog";
import React, { FC } from "react";

type Props = {
  block: TabsBlock;
};

const TabsBlockComponent: FC<Props> = ({ block }) => {
  return <div>TabsBlockComponent</div>;
};

export default TabsBlockComponent;
